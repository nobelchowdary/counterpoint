# Optional: required only for a future live GPT-5.6 analysis route.
# Never expose this value to the browser or commit it.
OPENAI_API_KEY=

# Optional: leave unset to use GPT-5.6 Terra.
OPENAI_MODEL=gpt-5.6-terra
